
登录接口（已有）
登出接口（待测）

新添加主机不下发插件

上传插件（已有）
下载插件（忽略）
获取插件基本信息（已有）
删除插件（已有）

新增一个 APP（已有）
删除一个 APP（已有）
获取 App（已有）
下发通用配置（已有）
下发白名单配置（已有）
下发算法配置（已有）
下发报警配置（忽略）

获取 APP 下的所有插件（已有）
获取 APP 下选中的插件（已有）
设置 APP 下选中的插件（已有）
测试邮件报警（忽略）
测试钉钉报警（忽略）
测试 HTTP 报警（忽略）

按条件获取 RASP（忽略）
删除一个 RASP（忽略）

生成/更新 token（待测）
获取所有 token（待测）
删除token（待测）

请求统计信息聚合（待测）
按照时间聚合攻击日志（待测）
按照类型聚合攻击日志（待测）
按照 UA 聚合攻击日志（待测）
攻击报警搜索 / csv导出（待测）
基线报警搜索 / csv导出（待测）

测试重复报警、报警丢失等